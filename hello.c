#include <stdio.h>

int main(int agrc, char * argv[]) {
    int a = 3;
    int b = 12;
    int c = 0;
    printf("Here's some more code we are adding");
    printf("Hello git!\n");
    printf("a = %d\nb = %d\nc = a + b = %d + %d = %d\n", a, b, a, b, a+b);
    return 0;
}
